<?php

return array(

    /*
    |--------------------------------------------------------------------------
    | Force redirect to secure URL.
    |--------------------------------------------------------------------------
    | Enabling this option allows the application to redirect the user to
    | the SSL enabled ('https') URL of the page for every request.
    |--------------------------------------------------------------------------
    */
    'force_secure' => false,

);
